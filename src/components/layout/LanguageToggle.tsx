// This component is no longer needed as we're using English only
// We'll keep an empty component to avoid breaking imports elsewhere
import { Button } from "@/components/ui/button";

export function LanguageToggle() {
  return null;
}
